{
  "name": "custom-card-proxy",
  "version": "2.0.0",
  "private": true,
  "description": "Tiny backend proxy that holds a Gemini API key and turns a player's card idea into a real in-game card.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
